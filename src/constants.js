export const KEYFRAMES_TYPES = {
  COMPLEX: "Complex",
  SIMPLE: "Simple"
}

// True if scalar values used for component to/from options, otherwise uses Coordinate values
export const TRANSFORM_COMPONENT_TYPES = {
  scale: false,
  rotate: true,
  translate: false,
  skew: false
}

export const EASING_TYPES = ['bounce', 'sway', 'hardbounce', 'hardsway'];